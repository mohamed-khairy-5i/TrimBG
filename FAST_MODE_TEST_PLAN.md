# خطة اختبار وضع المعالجة السريعة في TrimBG

## النطاق

تتحقق هذه الخطة من إضافة اختيار «المعالجة السريعة» باستخدام U2NetP FP16 مع إبقاء ISNet وضع «أفضل جودة». وتشمل الخطة build وTypeScript، ظهور الاختيار، الرفع والمعالجة محلياً، إنتاج PNG، سلامة التنقل وإعادة الضبط، وعدم وجود أخطاء runtime في المسار الأساسي.

خارج النطاق: إثبات أن U2NetP FP16 يساوي ISNet بصرياً، أو ضمان زمن ثابت على كل أجهزة المستخدمين، أو اختبار المتصفحات والأجهزة غير المتاحة في بيئة Chromium المحلية.

## معايير القبول

| المطلب | الاختبار | النتيجة المتوقعة | الدليل الحالي | الحالة |
|---|---|---|---|---|
| ظهور وضعين واضحين قبل الرفع | فتح `/` | يظهر «أفضل جودة — ISNet» و«المعالجة السريعة — U2NetP FP16» | لقطة الصفحة الرئيسية بعد build | Passed |
| بقاء ISNet هو الوضع الافتراضي | رفع صورة دون تغيير الوضع | تظهر النتيجة ويظهر في workspace وسم «أفضل جودة — ISNet» | اختبار rabbit عبر `/workspace` | Passed |
| توجيه الوضع السريع إلى FP16 | اختيار الوضع السريع ثم رفع الصورة | تظهر النتيجة ويظهر وسم «المعالجة السريعة — U2NetP FP16» | اختبار rabbit عبر المسار الفعلي | Passed |
| المعالجة محلية | فحص مسار المحرك والواجهة | لا يوجد رفع للصورة إلى خادم معالجة؛ يستخدم ONNX Runtime Web وملفات النموذج المحلية | `src/lib/u2netpBrowser.ts` و`WorkspaceContext.tsx` | Passed by code review |
| إنتاج نتيجة قابلة للتنزيل | اكتمال المعالجة | يظهر زر تنزيل PNG وتوجد صورة `After` | اختبارا ISNet وU2NetP FP16 | Passed |
| عدم تسرب الحالة بين الصور | العودة للرئيسية ثم رفع صورة جديدة | لا تمنع `processedImage` القديمة المعالجة الجديدة | إصلاح `reset()` واختبار العودة/الرفع | Passed |
| عدم الاحتفاظ بروابط Blob القديمة | تنفيذ reset | تُلغى روابط الصور القديمة عند كونها Blob URLs | `WorkspaceContext.tsx` | Passed by code review |
| منع نتيجة متأخرة بعد reset | تغيير الصورة أو العودة أثناء المعالجة | لا تُكتب نتيجة العملية الملغاة في الحالة الجديدة | `operationIdRef` داخل `WorkspaceContext.tsx` | Passed by code review |
| نجاح البناء | `npm run build` | ينجح Vite build دون خطأ | تشغيل 15 Aug 2026 | Passed |
| سلامة الأنواع | `npx tsc --noEmit -p tsconfig.app.json` | لا توجد أخطاء TypeScript | تشغيل 15 Aug 2026 | Passed |
| جودة سريعة عملية | AIM-500 وعينات شعر/فرو/أجسام صغيرة | MAE مقبول نسبياً ونتيجة مرئية قابلة للاستخدام مع توثيق القيود | MAE `0.050497`؛ فحص `aim_0406`, `aim_0448`, `aim_0462` | Passed with limitation |
| عدم استبدال الجودة دون دليل | مقارنة ISNet/U2NetP FP16 | يبقى ISNet الخيار الافتراضي لأن FP16 يفقد تفاصيل دقيقة أكثر | سجل المقارنة في `performance-findings.md` | Passed |

## اختبارات التنفيذ

### Static وBuild

تم تشغيل `npm run build` ونجح بناء Vite. ظهرت تحذيرات حجم chunks وقدم بيانات Browserslist، لكنها ليست أخطاء build. وتم تشغيل `npx tsc --noEmit -p tsconfig.app.json` ونجح دون أخطاء.

### Smoke وIntegration

تم فتح الصفحة الرئيسية بعد build، واختيار كل وضع، ورفع عينة AIM-500 `aim_0448_o_e4f47cba.jpg` عبر عنصر file input الحقيقي. في وضع ISNet اكتملت المقارنة وظهر زر تنزيل PNG ووسم الجودة. وفي وضع U2NetP FP16 اكتملت المقارنة وظهر وسم السرعة وزر تنزيل PNG.

### الأداء

في benchmark المعزول، سجل U2NetP FP16 بعد التهيئة `inferenceMs=625` و`totalMs=715`. وفي مسار التطبيق الكامل، سجلت إعادة اختبار دافئة `inferenceMs=615.6` و`totalMs=797.3`. هذه أرقام جلسة Chromium محلية وليست ضماناً لجميع الأجهزة.

### Regression للحالة

كان المسار معرضاً لبقاء `processedImage` القديمة عند العودة للرئيسية ثم اختيار صورة جديدة. عولج ذلك باستدعاء `reset()` قبل تثبيت الصورة الجديدة، وباستدعاء reset في روابط العودة من workspace، مع إلغاء Blob URLs القديمة. كما يمنع `operationIdRef` نتيجة عملية قديمة من الكتابة إلى الحالة بعد reset أو تغيير الصورة. يجب إعادة تنفيذ هذه الاختبارات بعد أي تغيير على التنقل أو سياق workspace.

## حالات الفشل وEdge Cases المطلوبة لاحقاً

ينبغي اختبار ملف غير مدعوم، ملف أكبر من 10 MB، صورة PNG شفافة، صورة شديدة الأبعاد، إعادة اختيار صورة أثناء المعالجة، وفشل تحميل نموذج U2NetP أو ملفات WASM. هذه الحالات لم تُنفذ في جلسة التحقق الحالية، ولذلك لا تُسجل كنجاح.

كما ينبغي اختبار WebGPU وCPU في متصفحات فعلية مختلفة، لأن زمن inference وعدد خيوط WASM وتوافر WebGPU تختلف حسب الجهاز. الاختبار الحالي يعتمد على Chromium المحلي مع `crossOriginIsolated=true`.

## بوابة الإصدار

يمكن اعتماد وضع السرعة كخيار اختياري محلي مع إبقاء ISNet افتراضياً، لأن اختبارات build وTypeScript وsmoke وintegration نجحت، ولأن القيود البصرية موثقة في الواجهة والتقارير. لا يجوز استبدال ISNet أو تسمية U2NetP FP16 «أفضل جودة» قبل توفر تقييم مستقل أوسع يثبت ذلك.

## الأوامر

```bash
npm run build
npx tsc --noEmit -p tsconfig.app.json
git diff --check
```

## الملفات المرجعية

- `src/context/WorkspaceContext.tsx`
- `src/lib/u2netpBrowser.ts`
- `src/pages/Index.tsx`
- `src/pages/Workspace.tsx`
- `performance-findings.md`
- `TRIMBG_MODEL_RESULTS.md`
- `research/pretrained-candidates.md`
