# التقرير النهائي لتدريب نموذج إزالة الخلفية

## الملخص التنفيذي

أُجريت دورة تدريب كاملة لتحسين نموذج LiteMatting المخصص لموقع TrimBG مع إبقاء المعالجة داخل متصفح المستخدم. شملت الدورة تدقيق البيانات، توليد بيانات تركيبية بتوزيع أحجام أفضل، تدريب V4 عند 128 و160 بكسل، تقطير المعرفة من U2NetP، تدريباً هجيناً يجمع الأقنعة الحقيقية مع أقنعة المعلم، loss موجهة للحواف، وتجربة زيادة سعة الشبكة.

أفضل نتيجة مستقلة داخل عائلة LiteMatting كانت تقريباً **MAE = 0.12426** في النموذج الهجين المقطر من U2NetP، بينما بقيت المقارنة البصرية على الشعر والفرو والخلفيات المعقدة تُظهر هالات وتسرباً واضحاً. لذلك لم ينجح النموذج المخصص في تجاوز U2NetP FP16، ولم يتم دمجه في الموقع.

القرار الإنتاجي النهائي هو إبقاء **ISNet** كخيار «أفضل جودة»، واستخدام **U2NetP FP16** كخيار «المعالجة السريعة». هذا القرار يقلل الزمن من نحو 6.4 ثانية في ISNet على بيئة الاختبار إلى نحو 0.6–0.8 ثانية في وضع U2NetP، مع تنازل معروف في التفاصيل الدقيقة.

## نتائج التجارب

| التجربة | الإعداد | نتيجة AIM-500 | الحجم أو الكلفة | القرار |
|---|---|---:|---:|---|
| V4-128 | 128px، عرض 2x | mean MAE = 0.12482 | ONNX نحو 1.05 MiB؛ CPU نحو 7.46 ms محلياً | بحثية فقط |
| V4-160 | 160px، عرض 2x | mean MAE = 0.15245 | CPU نحو 9.66 ms محلياً | مرفوضة |
| Hybrid distillation | U2NetP teacher + ground truth | mean MAE = 0.12426 | ONNX نحو 1.05 MiB؛ CPU نحو 8.62 ms محلياً | أفضل LiteMatting، غير إنتاجية |
| V5-192-lite | 192px، عرض 1x | mean MAE = 0.22471 | ONNX نحو 303 KiB؛ CPU نحو 12.16 ms محلياً | مرفوضة |
| Hard-edge loss | 128px، BCE أعلى | أفضل MAE داخلي نحو 0.13771 | كلفة تدريب أعلى دون تحسن بصري | مرفوضة |
| Width 3.0 | 128px، batch صغير | أول epoch: MAE = 0.25754 | 641.7 ثانية لأول epoch | أُوقفت |
| U2NetP FP16 | نموذج pretrained | MAE = 0.050497 | نحو 2.25 MiB؛ 615.6 ms inference في التطبيق | خيار السرعة |
| ISNet | نموذج الإنتاج الحالي | الأفضل بصرياً | نحو 6.4 ثانية warm في البيئة الحالية | خيار الجودة |

## ما الذي تعلمناه

رفع الدقة وحده لم يحل مشكلة الهالات. كما أن زيادة عدد النسخ التركيبية لا تعادل تنوع الصور الحقيقية أو دقة أقنعة alpha، خصوصاً في الشعر والفرو والأجسام ذات الحدود شبه الشفافة. تقطير U2NetP حسّن الرقم قليلاً، لكنه لم يحوّل النموذج الصغير إلى بديل بصري للمعلم.

تجربة loss الحواف لم تكن كافية لأن المشكلة ليست في معيار الخسارة فقط؛ فالنموذج يفتقد سعة وتمثيلاً وتنوعاً كافياً للحالات الصعبة. أما زيادة عرض الشبكة إلى 3.0 فرفعت كلفة التدريب بشدة ولم تقدم إشارة جودة، لذلك لا توجد مبررات للاستمرار في توسيع LiteMatting الحالي.

## الملفات المهمة

يُحفظ أفضل checkpoint بحثي للهجين في:

`experiments/lite-hybrid-distill-128/lite_matting_v3_128.onnx`

ويُحفظ checkpoint V4-128 في:

`experiments/lite-v4-screen/`

أما الأوزان والبيانات ونتائج التجارب فتبقى خارج Git عمداً لأنها ملفات كبيرة أو ناتجة عن التجارب. أدوات إعادة التوليد والتقييم موجودة داخل `scripts/`، والتغييرات البرمجية والتوثيق المتعقبان مثبتان في commit محلي:

`3db41b3 — Document custom matting training results`

## التوصية التالية

لا يُنصح بتدريب LiteMatting إضافياً بنفس المعمارية والوصفة. إذا كان المطلوب تحسين الجودة بعد هذه الدورة، فالمسار المنطقي هو استخدام معماريّة pretrained أقوى أو جمع مجموعة alpha حقيقية موجهة تحديداً إلى الحالات التي تفشل فيها U2NetP، ثم إجراء distillation جديد على تلك الحالات. إلى أن تتوفر هذه البيانات، فإن وضعَي ISNet وU2NetP FP16 هما الحل العملي الأكثر توازناً للموقع.
